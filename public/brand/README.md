Place production logo assets here:

- `logo-light.png`: dark text/icon version for light mode.
- `logo-dark.png`: light text/icon version for dark mode.

Use transparent PNG files when available. The React app falls back to the existing `logo.png` if these files are not present.
